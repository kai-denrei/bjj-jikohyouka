# Verdict #5: Dashboard, Viz Tabs, Explicit Dimensions, Dark Redesign — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Post-sweep flow becomes a legible dashboard hub with three tabbed visualizations; the dimension model becomes explicit and visible; the app goes dark-by-default with a modern pass — per brief Appendix B verdict #5 (2026-07-20).

**Architecture:** Token-level theme flip (no component forks — components already consume CSS custom properties); one new `Dashboard` screen replacing InterimScreen as the post-sweep home; a `VizTabs` component hosting Radar + two new SVG charts; `shortName` finally moves into `categories.json` (kills the Radar SHORT_NAME debt). Content model changes are PROPOSALS for Gerald (docs/dimensions-proposal.md), not unilateral category surgery.

**Tech Stack:** existing (React/TS/vitest/zod). No new deps. Hand-rolled SVG.

## Global Constraints

- **Dark palette (binding, "same gi dyed deeper"):** ground `--mat:#1B2130`; raised surface `--surface:#232B3D`; ink `--ink:#EDEAE2` (light theme's canvas becomes the text); secondary `--ink-2:#9BA3B4`; hairline `--line:#39415A`; strong line `--line-strong:#C7CBD6`; accent `--accent:#8FA7E8` (washed indigo — NOT acid green/vermilion). Belt tokens dark-adapted: `--belt-white:#E9E7E0`, `--belt-blue:#5B84CE`, `--belt-purple:#9067C6`, `--belt-brown:#A0714A`, `--belt-black:#14161C` + rule: black-belt curve/dot/swatch gets `stroke: var(--line-strong)` (the inverse of white-belt's treatment on light ground). Chips/cards sit on `--surface` with `--line` borders. `.chip` background becomes `var(--surface)` (the `#fff` literal dies). Focus ring stays `--accent`. Fonts unchanged (Bricolage/Atkinson/Plex Mono). No pure #000 anywhere; no new hues beyond the palette.
- Curve fills may rise to `fillOpacity 0.34` on dark; axis wash `--accent` at 0.10.
- **Signature discipline:** the belt-stripe bar and the bell-curve axis remain the identity; the dashboard frames them, nothing new competes.
- Copy sentence case, active voice; all NEW question/dimension content stays `draft`/proposal — no category creation without Gerald's sign-off.
- `shortName` added to categories.json + CategorySchema (optional string ≤14 chars); Radar/heatmap/depth chart consume it with name-fallback; Radar's hardcoded SHORT_NAME map DELETED.
- Every task: FULL `npm test` + `npm run build` + (content-touching) `bank:validate` 0 errors green before commit. Commit trailer: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

## File Structure

```
src/styles/tokens.css                    # dark tokens (Task 1)
src/data/question-bank/categories.json   # + shortName per category (Task 1)
src/lib/bank/schema.ts                   # CategorySchema.shortName (Task 1)
src/components/results/Radar.tsx         # SHORT_NAME map → bank shortName (Task 1)
src/components/results/DepthBars.tsx     # inverted depth chart (Task 2)
src/components/results/HeatMap.tsx       # band heat grid (Task 2)
src/components/results/VizTabs.tsx       # tablist hosting the three (Task 2)
src/components/Dashboard.tsx             # the hub (Task 3)
src/components/DimensionsPanel.tsx       # "What we measure" (Task 4)
docs/dimensions-proposal.md              # pressure/connection proposal for Gerald (Task 4)
src/App.tsx                              # flow rewiring (Tasks 3–4)
```

---

### Task 1: Dark tokens + shortName debt kill

**Files:** `src/styles/tokens.css`, `src/data/question-bank/categories.json`, `src/lib/bank/schema.ts`, `src/components/results/Radar.tsx`, `src/components/inputs/BellCurveAxis.tsx` (black-curve stroke rule + fill 0.34 + wash 0.10), any `#fff`/light-literal cleanup (`grep -rn '#fff\|#F4F4F1' src/`), test updates.

**Contract:**
- tokens.css `:root` replaced with the dark palette above (this IS the default — no media query, no toggle; light mode is gone until requested).
- `[data-belt="black"]` swatches/dots get `border/stroke: var(--line-strong)`; white-belt keeps a subtle `--line` stroke (both extremes outlined, middle belts self-sufficient).
- categories.json: every category gains `shortName` (author them: `Takedowns`, `Guard Pass`, `Open Guard Top`, … ≤14 chars, Title Case; meta_qualities → `Meta`, reputation → `Reputation`). Schema field optional, `.max(14)`.
- Radar: delete SHORT_NAME; label = `category.shortName ?? name`. NOTE: Radar receives `CategoryScore[]` which has `name` but not shortName — extend `CategoryScore` with `shortName?: string` (score.ts copies it from the bank category). Tests updated.
- Tests: tokens smoke (getComputedStyle unavailable in jsdom — assert the css file contains the binding hex values via a small fs read test in `src/styles/tokens.test.ts`); Radar labels use shortName from a fixture.

- [ ] RED (tokens.test.ts + Radar shortName test + score.ts CategoryScore extension test) → implement → GREEN → full gates → commit `feat(design): dark-by-default tokens — the same gi, dyed deeper`

### Task 2: VizTabs — Spider / Depth / Heat

**Files:** create `DepthBars.tsx`, `HeatMap.tsx`, `VizTabs.tsx`; tests `src/components/results/viz.test.tsx`.

**Contracts (all consume `categories: CategoryScore[]`, render only scored positional categories unless noted):**
- `DepthBars`: SVG, bars HANG from a top baseline (depth-sounding metaphor — deeper bar = deeper game): bar length ∝ score, sorted desc, `--accent` fill 0.75 opacity, shortName vertical-ish labels or under-bar mono initials (design for 390px: ≤15 bars × ~20px), score as mono number at bar tip, unscored categories rendered as empty hatch slots at the end. Baseline hairline `--line-strong`.
- `HeatMap`: grid of cells (5 per row), one per positional category incl. UNSCORED (hatch/outline only); fill = single-hue indigo ramp by band: Unmapped `--surface`, Learning 0.25 accent, Drilling 0.45, Positional 0.65, Rolling 0.85, Weapon 1.0 accent; cell shows shortName (10px mono) + optional score; `aria-label` per cell `"{name}: {band ?? 'not yet mapped'}"`.
- `VizTabs`: `role="tablist"` with three `.mono` tabs `Spider · Depth · Heat`, keyboard arrow switching, selected tab underlined `--accent`; hosts Radar / DepthBars / HeatMap; remembers selection in localStorage (`skillcheck.viztab.v1`).
- Tests: tab switching renders the right chart (each chart has a distinguishing testid); DepthBars sorts desc and hangs (first bar's rect y == baseline, height ∝ score); HeatMap renders one cell per positional category incl. null-score with the not-yet-mapped label; keyboard arrows move tab focus.

- [ ] RED → implement → GREEN → gates → commit `feat(results): tabbed visualizations — spider, depth bars, heat map`

### Task 3: Dashboard hub

**Files:** create `Dashboard.tsx`; modify `App.tsx`; tests App.flow + component.

**Contract:**
- Replaces InterimScreen as the post-sweep home (delete InterimScreen + its tests after parity; the radar-first requirement carries over via VizTabs defaulting to Spider).
- Layout: `VizTabs` block on top → `Deep dives` section: one row per positional category (in category order): shortName + band chip (mono) + drill-down state — `answered/total` mono when available (`drilldownQuestions` counts vs answered qids), `Not yet written` quiet text when the category has no available drill-downs, tap row → category run (only when available); completed rows get a filled dot. Meta section beneath (same row treatment for meta_qualities when it has availability). Footer buttons: `Full report` (→ results screen) and `What we measure` (→ DimensionsPanel, Task 4 wires it — render the button disabled-hidden until then if sequencing demands, better: Task 3 adds the button behind a prop and Task 4 fills it).
- Navigation: after sweep completion → dashboard; category run completion → dashboard (not interim); mid-drilldown Pause → dashboard; results `Back to categories` → dashboard; tapping the belt-stripe bar → dashboard (when a report exists) — the bar becomes the persistent way home; aria: bar wrapped in a button with `aria-label="Back to your map"` only when clickable.
- App state: `screen` value `interim` renamed `dashboard` (adjust tests); report recomputed on every arrival (existing scoreAnswers path).
- Tests: post-sweep lands on dashboard with tabs + rows; row shows answered-count after completing a drill-down; unavailable category row not clickable + labeled; bar-tap returns to dashboard from results; Pause mid-drilldown → dashboard.

- [ ] RED → implement (delete InterimScreen last, after flow tests green) → GREEN → gates → commit `feat(app): dashboard hub — the map you fill in`

### Task 4: Explicit dimensions + proposal for Gerald

**Files:** create `DimensionsPanel.tsx`, `docs/dimensions-proposal.md`; modify `Dashboard.tsx` (wire button), `src/data/question-bank/categories.json` (descriptions audit — fill any missing `description` with one observable-events line, ≤120 chars, draft-spirit).

**Contract:**
- DimensionsPanel: full-screen panel (same dialog pattern as InfoPanel): heading `What we measure`; intro line verbatim: `Fifteen positional situations, plus the qualities that cut across all of them. This map is version 0.2 — it will be wrong in places, and your feedback reshapes it.`; then the positional list (shortName bold + description line each), then `Cross-cutting qualities` section listing the meta-qualities items' quality names (pressure, connection, composure, timing, chaining, defense depth, adaptability — derive from the meta questions' whats, not hardcoded prose where avoidable... hardcode the seven labels WITH a code comment pointing at meta-qualities.json as source), each with its one-line item text; closing line: `Missing something you rate in training partners? That's exactly the feedback that changes the map.`
- `docs/dimensions-proposal.md` (~1 page, for Gerald): the model question stated plainly — are pressure/connection/etc. (a) items inside one meta bucket (status quo), (b) first-class dimensions with own drill-downs + viz axes, or (c) a cross-cutting overlay scored from tagged items across categories. Lay out costs (content volume for (b), schema `quality` tag for (c)), recommend (c) as the v0.3 direction with reasoning, END with `Decision: ____` blank. Committed for his review; no schema/content changes in this task beyond description fills.
- Tests: panel opens from dashboard, lists 15 positional + 7 qualities, closes on Escape.

- [ ] RED → implement → GREEN → gates (+ `bank:validate` if categories.json touched) → commit `feat(app): the dimension map made explicit + proposal for the model decision`

### Task 5: Screenshots + final review + merge

- Rerun walkthrough (dark UI now): refresh 01–08 + new `09-dashboard.png`, `10-viz-depth.png`, `11-viz-heat.png`, `12-dimensions.png` (script additions; ONE chromium; LOOK at every PNG — dark contrast, black-belt curve visibility on dark is the #1 check).
- Whole-branch review (fable): dark-contrast a11y (WCAG on new pairs), flow coherence (no orphaned interim references), taxonomy copy fidelity, deferred-list triage. Fix waves per protocol. Merge on Yes.

## Definition of Done

- Dark by default everywhere incl. widget/radar/badge; black-belt marks visible; no light-theme literals left.
- Post-sweep: dashboard with three working viz tabs; every category row shows its deep-dive state; navigation always finds home.
- Dimensions visible in-app; pressure/connection model decision written up for Gerald, not made for him.
- Screenshots current; 190+ tests green.
